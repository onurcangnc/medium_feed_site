<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ site_title }}</title>

  <meta name="description" content="Explore Medium posts by Onurcan Genç. CTFs, pentests, real-world cybersecurity content." />
  <link rel="canonical" href="https://onurcangenc.com.tr/" />
  <link rel="icon" href="/static/profile.jpg" type="image/png" />
  <link rel="stylesheet" href="/static/output.css" />
  <script src="https://unpkg.com/scrollreveal"></script>
</head>
<body class="bg-gray-950 text-white font-sans leading-relaxed">

  <header class="p-6 md:p-10 flex flex-col md:flex-row justify-between items-center">
    <div>
      <h1 class="text-3xl md:text-4xl font-bold">Onurcan Genç</h1>
      <p class="text-gray-400 text-sm md:text-base">Cybersecurity Analyst | CTF Enthusiast | Penetration Tester</p>
    </div>
  </header>

  <section class="px-6 md:px-10 py-12 text-center">
    <h2 class="text-4xl font-extrabold mb-4 tracking-tight">Medium Articles</h2>
    <p class="text-gray-400 max-w-2xl mx-auto">
      Writeups, stories, and technical analysis directly from my Medium feed.
    </p>
  </section>

  <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 px-6 md:px-10 py-10">
    {% for post in posts %}
    <div class="rounded-xl shadow-lg overflow-hidden bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 hover:scale-[1.01] transition duration-300" data-sr>
      {% if post.thumbnail %}
      <div class="w-full h-48 overflow-hidden">
        <img src="{{ post.thumbnail }}" alt="Thumbnail for {{ post.title }}" class="w-full h-full object-cover" loading="lazy">
      </div>
      {% endif %}
      <div class="p-5">
        <h3 class="text-xl font-semibold text-white mb-2">{{ post.title }}</h3>
        <p class="text-gray-400 text-sm mb-3">{{ post.summary }}</p>
        <a href="{{ post.link }}" target="_blank" rel="noopener" class="text-cyan-400 text-sm font-medium hover:underline">Read more →</a>
      </div>
    </div>
    {% endfor %}
  </section>

  {% if total_pages > 1 %}
  <div class="flex justify-center mt-8 space-x-2">
    {% for page in range(1, total_pages + 1) %}
      <a href="{{ 'index.html' if page == 1 else 'page/' ~ page ~ '.html' }}"
        class="px-4 py-2 rounded-lg border border-gray-700 transition duration-200
                {% if page == current_page %}
                  bg-cyan-600 text-white font-bold
                {% else %}
                  bg-gray-800 text-gray-300 hover:bg-gray-700 hover:text-white
                {% endif %}">
        {{ page }}
      </a>
    {% endfor %}
  </div>
  {% endif %}

  <footer class="text-center py-8 px-4 text-sm text-gray-500 border-t border-gray-700">
    © 2025 Onurcan Genç. All rights reserved.
  </footer>

  <script>
    ScrollReveal().reveal('[data-sr]', {
      origin: 'bottom',
      distance: '30px',
      duration: 800,
      delay: 100,
      reset: false
    });
  </script>
</body>
</html>
