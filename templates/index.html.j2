<!DOCTYPE html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ site_title }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white font-sans">
    <div class="max-w-6xl mx-auto px-4 py-10">
      <h1 class="text-3xl font-bold mb-8 text-center">{{ site_title }}</h1>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {% for post in posts %}
        <div class="bg-gray-800 rounded-xl shadow-md p-6 hover:shadow-lg transition">
          <div class="text-sm text-gray-400 mb-2">{{ post.published }}</div>
          <h2 class="text-xl font-semibold text-white mb-2">
            {{ post.title }}
          </h2>
          <p class="text-gray-300 text-sm mb-4">
            {{ post.summary | truncate(150, True, "...") }}
          </p>
          <a href="{{ post.link }}" target="_blank" class="inline-block text-blue-400 hover:underline text-sm">Read Article →</a>
        </div>
        {% endfor %}
      </div>

      {% if total_pages > 1 %}
      <div class="mt-10 text-center space-x-2">
        {% for page_num in range(1, total_pages + 1) %}
          {% if page_num == current_page %}
            <span class="px-3 py-1 rounded bg-blue-600 text-white">{{ page_num }}</span>
          {% else %}
            <a href="{% if page_num == 1 %}/index.html{% else %}/page/{{ page_num }}.html{% endif %}" class="px-3 py-1 rounded bg-gray-700 hover:bg-gray-600">{{ page_num }}</a>
          {% endif %}
        {% endfor %}
      </div>
      {% endif %}
    </div>
  </body>
</html>
